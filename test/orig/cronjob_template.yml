metadata:
  name: {job_name}
spec:
  schedule: "{cron}"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: {job_name}
            args:
            - {url}
