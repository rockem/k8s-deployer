metadata:
  annotations:
      external-dns.alpha.kubernetes.io/hostname: external
      {internalLoadBalancerEntry}
      {prometheusPortEntry}
      {prometheusScrapeEntry}
spec:
  selector:
    color: {serviceColor}
  ports:
    - name: tcp-80
  type: {serviceType}