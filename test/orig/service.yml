metadata:
  annotations:
      external-dns.alpha.kubernetes.io/hostname: external
      {internalLoadBalancerEntry}
spec:
  selector:
    color: {serviceColor}
  ports:
    - name: tcp-80
  type: {serviceType}