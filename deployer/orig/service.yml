kind: Service
apiVersion: v1
metadata:
  name: {name} #configured
  labels:
    name: {name}
spec:
  selector:
    name: {name} #configured
  ports:
  - protocol: TCP
    targetPort: 8080
    port: 80
  type: LoadBalancer
